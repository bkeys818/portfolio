#!/usr/bin/env bash
# Purpose: Compile files for website
# Author: Ben Keys
# --------------------------------------

sass --embed-source-map root:root && \

find root -type f -name *.css | while read -r file; do
    [[ -f "${file%???}scss" ]] && rm "${file%???}scss"
done